 
<?php
	//this page allows users to put items into a shopping cart
	include_once('webconfig.php');
	include_once(ABSOLUTE_PATH . '/classes/product.class.php');
	include_once(ABSOLUTE_PATH . '/classes/fruit_product.class.php');
	include_once(ABSOLUTE_PATH . '/classes/veggie_product.class.php');
	session_start();
?>


<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<title>I've Got A Lovely Bunch of Coconuts: Cart!</title>
<link type="text/css" rel="stylesheet" href="<?php echo URL_ROOT . '/styles.css' ?>" />
</head>

<body>

<p><b>Your Cart</b></p>

<?php
        // Error: $aryCartArray not necessary because aryCartArray does not exist

	//get the product array stored in session
	$aryProductsArray = unserialize($_SESSION['aryProductArray']);

	//for each item in the cart array, loop through and write out the quantity and item name
	// Error: $aryCartArray should be $aryProductsArray because you are looping over this array. Fix by replacing $aryCartArray with $aryProductsArray
	for ($x=0; $x < count($aryProductsArray); $x++) {
		// Error: You are selecting an element from $aryProductsArray not $aryCartArray which does not exist. Replace $aryCartArray with $aryProductsArray
		$aryCartItemArray = $aryProductsArray[$x];

                print_r($aryCartItemArray)."<br><br>";
		// find the corresponding product in the product array
                // Error: Missing semi-colon at end of assignment statement. Place semi-colon at the end to fix it.
                // Error: $thisProduct is the $aryCartItemArra, not an element of the $aryProductArray. Fix by removing $aryProductArray.
                // Error: Remove index because $aryCartItemArray is more similar to an associative array not an index array.
		$thisProduct = $aryCartItemArray;
		// Error: Must use the get accessor for product name and quantity since the property is protected
		//echo "<b>" . $thisProduct->__get('product_name') . "</b>" . " Qty: " . $aryCartItemArray[1] . "<br />";
		
	}

?>

<br />
<br />
<br />
<a href="index.php">Start all over again</a><br />
<i>This will reset your inventory.</i><strong></strong>

</body>
</html>